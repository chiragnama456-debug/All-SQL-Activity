use world;
select distinct(continent) from country;
select continent from country group by continent;
 
select count(name), count(distinct(name)) from country;
select name, count(name) from country group by name;

select count(name) from city where district='zuid-holland';
select district, count(name), sum(population) from city group by district;

-- count the number of countries which have a life expectancy from 70.1 to 83.5
-- get the total population of the country to got there independence after 1990
-- count the number of countries which are not slaved by any community
-- get the total countries and the total surfacearea for each region 
-- get the total countries for each continent
-- get the total country for each region for each continent

-- q1
select count(name) from country where lifeexpectancy between 70.1 and 83.5;
-- q2
select name,sum(population) from country where indepyear>=1990 group by name;
-- q3 not done
select count(name) from country where indepyear is null;
-- q4 
select count(name), sum(surfacearea) from country group by region;
-- q5
select continent,count(name) from country group by continent;
-- q6 
select region,continent,count(name) from country group by region,continent;
